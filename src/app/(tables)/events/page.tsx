import { EntityCard } from "@/src/components/cards/EntityCard";

export default function EventsPage() {
    return (
        <div className="p-8">
            <EntityCard entityId="event" />
        </div>
    );
}
